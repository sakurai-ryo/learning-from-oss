---
title: "現象から引く索引 — 症状 → 仕組み → ページ → 確認するビュー"
description: "この章は SQL が通る層を上から順に下ってきた。最後に、その順序を逆から引けるようにする。デッドロックが出た、ALTER が固まった、レプリカが遅れた、インデックスが使われない — 現象を起点に、それがどの層の話で、どのページで説明していて、どのビューを見れば裏が取れるかを一覧にする。"
group: "横断"
sidebar:
  order: 112
---

## 何を学んだか

この章の 114 ページは層の順に並んでいる。だが実務で起きるのは現象のほうが先だ。

このページは逆引きの索引になっている。**症状 → その症状を生む仕組み → 詳しいページ → 裏を取るビュー**の 4 列で引く。

引くときのコツが 1 つある。**同じ症状名が複数の層から出ることがある。** `Too many connections` は接続ハンドラと X Plugin の両方から出るし、`Lock wait timeout exceeded` は InnoDB の行ロックからも MDL からも出る (後者は正確には `ER_LOCK_WAIT_TIMEOUT` で、待っていたものが違う)。だから表は「候補の層」を複数挙げている場合がある。切り分け方も列に書いた。

## 接続・プロトコル

| 症状                                                          | 仕組み                                                                                                                        | ページ                                                       | 確認するビュー                                                                     |
| ------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------ | ---------------------------------------------------------------------------------- |
| `Too many connections`                                        | `max_connections + 1` まで通し、+1 枠は `CONNECTION_ADMIN` 用に認証後判定                                                     | [接続層](./connection-layer/)                                | `Threads_connected` / `Max_used_connections` / `Connection_errors_max_connections` |
| X Protocol で `Too many connections`                          | X の内部セッションも `srv_session_open` を通るので `mysqlx_max_connections` を通っても classic の `max_connections` に当たる  | [X Plugin](./x-plugin-session-and-sql/)                      | `Mysqlx_connections_rejected`                                                      |
| `MySQL server has gone away` / `Lost connection during query` | `wait_timeout` はタイマーではなく `do_command` が毎コマンド張り直す socket read timeout                                       | [接続層](./connection-layer/)                                | `Aborted_clients` / `performance_schema.threads`                                   |
| `Got a packet bigger than 'max_allowed_packet' bytes`         | 4 バイトヘッダの 3 バイト長 + `max_allowed_packet`。**サーバとクライアント両方の値が効く**                                    | [パケット](./packet-framing/)                                | `Bytes_received` / `ER_NET_PACKET_TOO_LARGE`                                       |
| `Got packets out of order`                                    | シーケンス番号のずれ。16MB 分割の途中で切れた、または並行送信                                                                 | [パケット](./packet-framing/)                                | エラーログ                                                                         |
| `Commands out of sync`                                        | `mysql_use_result` / ストリーミング中に同じ接続で次のクエリを送った                                                           | [クライアント側](./client-library-and-streaming/)            | アプリのログ                                                                       |
| 初回接続だけ遅い                                              | `caching_sha2_password` の full auth。非 TLS では RSA 公開鍵の往復が増える                                                    | [ハンドシェイクと認証](./handshake-and-auth/)                | `performance_schema.events_stages_*`                                               |
| 8.0 のクライアントが繋がらなくなった                          | 8.4 で `mysql_native_password` が `PLUGIN_OPT_DEFAULT_OFF`、`default_authentication_plugin` は `authentication_policy` に置換 | [ハンドシェイクと認証](./handshake-and-auth/)                | `SHOW PLUGINS`                                                                     |
| `Prepared statement needs to be re-prepared`                  | メタデータが変わると `Reprepare_observer` が再準備を要求。3 回失敗でエラー                                                    | [prepared statement](./binary-protocol-prepared-statements/) | `Com_stmt_reprepare`                                                               |
| `Can't create more than max_prepared_stmt_count statements`   | `max_prepared_stmt_count` はグローバル (既定 16382)。プールが接続ごとに PS をキャッシュすると数本で届く                       | [prepared statement](./binary-protocol-prepared-statements/) | `Prepared_stmt_count`                                                              |
| 行数は少ないのにクエリが遅い                                  | 遅いクライアントが送信でサーバスレッドを止めている                                                                            | [行の返送](./sending-rows-and-limit/)                        | `SHOW PROCESSLIST` の `State: Sending data`、`net_write_timeout`                   |

## 実行計画

| 症状                                          | 仕組み                                                                                                | ページ                                                                                          | 確認するビュー                                                                 |
| --------------------------------------------- | ----------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------ |
| インデックスがあるのに使われない (型)         | 暗黙の型変換で列が関数に包まれる。比較文脈では**定数側にしか変換器を挿さない**規則がある              | [名前解決と Item ツリー](./name-resolution-and-items/)                                          | `EXPLAIN` + `SHOW WARNINGS` (`ER_WARN_INDEX_NOT_APPLICABLE` は EXPLAIN 時のみ) |
| インデックスがあるのに使われない (照合)       | JOIN する 2 列の照合順序が違い、片側が変換される                                                      | [名前解決と Item ツリー](./name-resolution-and-items/)                                          | `Illegal mix of collations` / `SHOW CREATE TABLE`                              |
| インデックスがあるのに使われない (統計)       | `records_in_range` / `rec_per_key` の推定が実態とずれている                                           | [統計とコストモデル](./statistics-and-cost-model/) / [InnoDB 統計](./innodb-stats-and-metrics/) | `EXPLAIN` の `rows` と実測の乖離、`mysql.innodb_index_stats`                   |
| インデックスがあるのに使われない (区間)       | range 分析が使える区間を作れない。`LIKE '%abc'` はインデックス全体を覆う区間になりコストで負ける      | [range 分析](./range-optimizer/)                                                                | `EXPLAIN` の `possible_keys` と `key` の差                                     |
| `ANALYZE TABLE` すると実行計画が変わる        | persistent stats の再計算。`innodb_stats_auto_recalc` は行の 10% 変更で走る                           | [InnoDB 統計](./innodb-stats-and-metrics/)                                                      | `mysql.innodb_table_stats` の `last_update`                                    |
| 主キー検索で `Using index condition` が出ない | **ICP はクラスタードインデックスには押し込まない**                                                    | [アクセスパスの選択](./access-path-selection/)                                                  | `EXPLAIN` の `Extra`                                                           |
| `Using MRR` が出ない                          | `choose_mrr_impl` が「テーブル > バッファプール」「50 行超」「PK でない・カバリングでない」を要求する | [アクセスパスの選択](./access-path-selection/)                                                  | `optimizer_switch` の `mrr`                                                    |
| JOIN 順序が意図と違う                         | 既定の `optimizer_search_depth = 62` は枝刈り付き全探索。コスト推定がずれれば順序もずれる             | [join 順序](./join-order-search/)                                                               | `optimizer_trace`                                                              |
| `FORCE INDEX` が効かない                      | 新構文 `/*+ INDEX() */` は `FORCE INDEX` 相当で、同居すると**新が古を完全に無効化する**               | [ヒントと optimizer_switch](./optimizer-hints-and-switches/)                                    | `SHOW WARNINGS` の `ER_UNRESOLVED_HINT_NAME`                                   |
| `EXPLAIN` が MDL 待ちで止まる                 | EXPLAIN も `unit->optimize()` まで本当に走る。ロックも取る                                            | [SELECT の一生](./life-of-a-select/)                                                            | `performance_schema.metadata_locks`                                            |
| `EXPLAIN ANALYZE` の行数が合わない            | `actual_rows` は `num_init_calls` で割った**1 ループあたりの平均**                                    | [EXPLAIN ANALYZE](./explain-analyze-and-tree/)                                                  | `EXPLAIN ANALYZE` の `loops`                                                   |
| `optimizer_trace` が途中で切れる              | `optimizer_trace_max_mem_size` 超過は破棄ではなく**切り詰め**                                         | [EXPLAIN ANALYZE](./explain-analyze-and-tree/)                                                  | `I_S.OPTIMIZER_TRACE` の `MISSING_BYTES_BEYOND_MAX_MEM_SIZE`                   |

## 実行 (ソート・一時表・JOIN)

| 症状                                       | 仕組み                                                                                     | ページ                                                                          | 確認するビュー                                                          |
| ------------------------------------------ | ------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------- | ----------------------------------------------------------------------- |
| `Using filesort`                           | インデックスから並びを得られなかった                                                       | [ORDER BY / GROUP BY](./sort-avoidance-and-ordering/) / [filesort](./filesort/) | `Sort_merge_passes` / `Sort_scan`                                       |
| ページネーションが深いページで急に遅くなる | `test_if_cheaper_ordering` の「WHERE と ORDER BY は無相関」仮定                            | [ORDER BY / GROUP BY](./sort-avoidance-and-ordering/)                           | `EXPLAIN` の `rows` と `LIMIT` の関係                                   |
| `Using temporary`                          | 集約・DISTINCT・UNION が内部一時表を要求した                                               | [内部一時表](./materialization-and-temptable/)                                  | `Created_tmp_tables`                                                    |
| `Created_tmp_disk_tables` が伸びる         | 一時表がメモリ上限を超えてディスクに落ちた                                                 | [内部一時表](./materialization-and-temptable/)                                  | `tmp_table_size` (テーブル単位) と `temptable_max_ram` (**グローバル**) |
| `The table is full`                        | TempTable が `temptable_max_ram` + `temptable_max_mmap` (既定 0) を使い切った              | [内部一時表](./materialization-and-temptable/)                                  | `Created_tmp_disk_tables`                                               |
| `Sort_merge_passes` が増える               | `sort_buffer_size` に収まらず、7 本ずつのマージを繰り返した                                | [filesort](./filesort/)                                                         | `Sort_merge_passes` / `Created_tmp_files`                               |
| `Using join buffer (hash join)`            | 内側の `type` が `ALL` / `index` / `range` / `index_merge` だった (等価条件で引けていない) | [join の実行](./join-iterators/)                                                | `EXPLAIN FORMAT=TREE` の `Inner hash join`                              |
| hash join が急に遅くなる                   | `join_buffer_size` に収まらず spill。chunk は最大 128 本                                   | [join の実行](./join-iterators/)                                                | `EXPLAIN ANALYZE` の実測時間                                            |
| BKA が使われない                           | `batched_key_access` は既定 off                                                            | [join の実行](./join-iterators/)                                                | `optimizer_switch`                                                      |

## ロックとトランザクション

| 症状                                             | 仕組み                                                                                     | ページ                                                                       | 確認するビュー                                                                        |
| ------------------------------------------------ | ------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------- | ------------------------------------------------------------------------------------- |
| `Deadlock found when trying to get lock`         | wait-for graph に閉路。**検出は待ち手ではなく背景スレッドが行う非同期処理**                | [デッドロック検出](./deadlock-detection/)                                    | `SHOW ENGINE INNODB STATUS` の LATEST DETECTED DEADLOCK、`innodb_print_all_deadlocks` |
| 同時 `INSERT` でデッドロック                     | 重複検査が next-key lock を取る。**`ON DUPLICATE KEY UPDATE` / `REPLACE` は S ではなく X** | [INSERT のロック](./insert-and-duplicate-check/)                             | 同上                                                                                  |
| `Lock wait timeout exceeded` (InnoDB 行ロック)   | `innodb_lock_wait_timeout` (既定 50 秒)                                                    | [ロックの種類](./lock-modes-and-types/)                                      | `performance_schema.data_lock_waits` / `sys.innodb_lock_waits`                        |
| `ER_LOCK_WAIT_TIMEOUT` だが行ロックではない      | MDL の待ち。`lock_wait_timeout` (既定 1 年)                                                | [MDL](./metadata-locking/)                                                   | `performance_schema.metadata_locks`                                                   |
| ロックしないはずの範囲がロックされる             | RR の next-key lock はギャップを含む。`LOCK_ORDINARY == 0` = フラグなしが next-key         | [ロックの種類](./lock-modes-and-types/) / [RR と RC](./locking-in-rr-vs-rc/) | `data_locks` の `LOCK_MODE` (supremum pseudo-record ならギャップ)                     |
| RC にしたらロック競合が減った                    | ギャップロックを省く判定が入る。ただし重複検査や外部キー検査では別ルール                   | [RR と RC](./locking-in-rr-vs-rc/)                                           | `data_locks` の `LOCK_MODE`                                                           |
| RC にしたら `ER_BINLOG_STMT_MODE_AND_ROW_ENGINE` | RC では `HA_BINLOG_STMT_CAPABLE` が落ちる                                                  | [RR と RC](./locking-in-rr-vs-rc/)                                           | `binlog_format`                                                                       |
| `AUTO_INCREMENT` が飛ぶ                          | `innodb_autoinc_lock_mode` と、失敗したトランザクションが採番を戻さない設計                | [INSERT のロック](./insert-and-duplicate-check/)                             | `SHOW CREATE TABLE` の `AUTO_INCREMENT=`                                              |
| `History list length` が伸び続ける               | 古い read view が生きている間、purge が undo を消せない                                    | [purge](./purge/) / [read view](./read-view-and-visibility/)                 | `SHOW ENGINE INNODB STATUS` の TRANSACTIONS、`I_S.INNODB_TRX` の `trx_started`        |
| 更新直後の走査だけ遅い                           | セカンダリインデックスの葉に版がないので、クラスタード側の版鎖を辿っている                 | [セカンダリインデックスと MVCC](./secondary-index-visibility/)               | `Innodb_rows_read` と返却行数の乖離                                                   |
| 大量 DELETE のロールバックが終わらない           | ロールバックは undo を 1 レコードずつ逆適用する。コミットより遅い                          | [コミットとロールバック](./commit-and-rollback-internals/)                   | `I_S.INNODB_TRX` の `trx_rows_modified`                                               |
| `SELECT` の結果が古い                            | RR では最初の読みでスナップショットが固定される。RC は文ごと                               | [read view](./read-view-and-visibility/)                                     | `START TRANSACTION WITH CONSISTENT SNAPSHOT` の有無                                   |

## 型・スキーマ定義

| 症状                                               | 仕組み                                                                                                | ページ                                                                                          | 確認するビュー                              |
| -------------------------------------------------- | ----------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------- | ------------------------------------------- |
| `Data truncated` が警告のときとエラーのときがある  | 厳格モードは `Strict_error_handler` が深刻度を昇格させる。`SELECT` とデータ変更でない文には積まれない | [sql_mode と厳格モード](./sql-mode-and-strict/)                                                 | `@@session.sql_mode`                        |
| `INSERT IGNORE` が想定より広く黙る                 | `Ignore_error_handler` の対象に外部キー違反・CHECK 違反・`NOT NULL` 違反が入っている                  | [sql_mode と厳格モード](./sql-mode-and-strict/)                                                 | `SHOW WARNINGS`                             |
| `Row size too large ... is 65535`                  | Server 側の `pack_length` 合計の検査。InnoDB の 8126 とは別の検査                                     | [型と Field クラス](./field-and-types/)                                                         | `SHOW CREATE TABLE`                         |
| 非厳格モードで長すぎるインデックスが作れてしまう   | `ER_TOO_LONG_KEY` も昇格対象。非厳格では警告 + プレフィックスに切り詰め                               | [sql_mode と厳格モード](./sql-mode-and-strict/)                                                 | `SHOW CREATE TABLE` のキー長                |
| `Specified key was too long`                       | キー長はバイト数。utf8mb4 は 1 文字 4 バイトで数える                                                  | [文字セットと照合順序](./charset-and-collation/) / [セカンダリインデックス](./secondary-index/) | `SHOW CREATE TABLE`                         |
| 5.7 から移行したら UNIQUE に重複が入るようになった | 8.0 既定の `utf8mb4_0900_ai_ci` は **NO PAD**。末尾空白が区別される                                   | [文字セットと照合順序](./charset-and-collation/)                                                | `SHOW TABLE STATUS` の `Collation`          |
| `ORDER BY` のメモリが想定より要る                  | ソートキーは元の文字列でなく weight 列。長さは `strnxfrmlen` で決まる                                 | [文字セットと照合順序](./charset-and-collation/) / [filesort](./filesort/)                      | `Sort_merge_passes`                         |
| 接続によって時刻の表示がずれる                     | `TIMESTAMP` はセッションのタイムゾーンで変換される。`DATETIME` はされない                             | [日付時刻とタイムゾーン](./datetime-and-timezone/)                                              | `@@session.time_zone`                       |
| `TIMESTAMP` に 2038 年以降が入らない               | 列型の上限が `int32` の最大値。`my_time_t` 自体は 64 ビット                                           | [日付時刻とタイムゾーン](./datetime-and-timezone/)                                              | `DATETIME` への変更を検討                   |
| `Unknown or incorrect time zone`                   | `mysql.time_zone*` が空。名前付きゾーンは投入が要る (`+09:00` なら不要)                               | [日付時刻とタイムゾーン](./datetime-and-timezone/)                                              | `SELECT COUNT(*) FROM mysql.time_zone_name` |
| 子テーブルへの `INSERT` が親の `UPDATE` を待つ     | 外部キー検査が親行に `LOCK_S` を取る。RR でもギャップは取らない                                       | [外部キー](./foreign-keys/)                                                                     | `data_locks` の `S,REC_NOT_GAP`             |
| `ON DELETE CASCADE` で消えた行がトリガに乗らない   | カスケードは InnoDB 内で完結し SQL 層に戻らない                                                       | [外部キー](./foreign-keys/) / [トリガ](./triggers-and-stored-programs/)                         | トリガでの監査ログの欠落                    |
| `ER_FK_DEPTH_EXCEEDED`                             | `FK_MAX_CASCADE_DEL = 15`。カスケードの再帰上限                                                       | [外部キー](./foreign-keys/)                                                                     | FK の連鎖を辿る                             |
| 関数インデックスが使われない                       | `WHERE` の式が定義の式と一致しないと使えない (`->>` は別の式)                                         | [生成カラムと関数インデックス](./generated-columns-and-functional-indexes/)                     | `SHOW CREATE TABLE` の `((...))`            |
| `Functional index on a lob` で作れない             | 式の結果型が BLOB / TEXT になっている。`CAST(... AS CHAR(N))` が要る                                  | [生成カラムと関数インデックス](./generated-columns-and-functional-indexes/)                     | `ER_FUNCTIONAL_INDEX_ON_LOB`                |
| JSON のキー順が入力と変わる                        | バイナリ形式が**長さ順・辞書順**でキーを保持する。重複キーも空白も消える                              | [JSON](./json-storage-and-partial-update/)                                                      | 元テキストが要るなら `TEXT`                 |
| 大きい JSON の 1 フィールド更新が遅い              | 部分更新の条件を外れて文書全体を書き直している                                                        | [JSON](./json-storage-and-partial-update/)                                                      | `optimizer_trace` の `cause`                |
| ビューが遅い                                       | 集約 / `UNION` / `LIMIT` / ウィンドウ関数があると MERGE できず実体化される                            | [ビュー](./views-and-view-security/)                                                            | `EXPLAIN` の `<derived>`、`SHOW WARNINGS`   |
| `DROP USER` したらビューが壊れた                   | `SQL SECURITY DEFINER` が既定。定義者の権限で実行される                                               | [ビュー](./views-and-view-security/)                                                            | `I_S.VIEWS` の `definer`                    |
| `sql_mode` を変えたのにトリガの挙動が変わらない    | 作成時の `sql_mode` と文字セットが凍結される                                                          | [トリガとストアドプログラム](./triggers-and-stored-programs/)                                   | `I_S.TRIGGERS` の `sql_mode`                |
| 触っていないテーブルがデッドロックに出る           | prelocking がトリガの参照先まで開いてロックする                                                       | [トリガとストアドプログラム](./triggers-and-stored-programs/)                                   | `SHOW ENGINE INNODB STATUS`                 |
| トリガの中の文がダイジェストに出ない               | パース時に PSI とダイジェストが `nullptr` に落とされる                                                | [トリガとストアドプログラム](./triggers-and-stored-programs/)                                   | `events_statements_history` の入れ子        |
| 同じクエリが接続によって成功したり失敗したりする   | セッション変数がプールの接続ごとに違う                                                                | [コネクションプールとセッション状態](./connection-pool-and-session-state/)                      | `performance_schema.variables_by_thread`    |
| `Table already exists` がたまに出る                | 一時テーブルが返却された接続に残っている                                                              | [コネクションプールとセッション状態](./connection-pool-and-session-state/)                      | `I_S.INNODB_TEMP_TABLE_INFO`                |
| `History list length` が伸び続ける (プール利用時)  | 返却前に `COMMIT` していない接続が read view を保持している                                           | [コネクションプールとセッション状態](./connection-pool-and-session-state/) / [purge](./purge/)  | `I_S.INNODB_TRX` の `trx_started`           |

## DDL

| 症状                                                | 仕組み                                                                                                                                       | ページ                                                                                | 確認するビュー                                                                        |
| --------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------- |
| ALTER が「固まる」、その後すべてのクエリが止まる    | 長いトランザクションが MDL を持っている → ALTER が排他 MDL 待ち → 後続が ALTER の後ろに並ぶ                                                  | [MDL](./metadata-locking/)                                                            | `performance_schema.metadata_locks`、`State: Waiting for table metadata lock`         |
| `ALGORITHM=INPLACE` なのに一瞬止まる                | **InnoDB は `HA_ALTER_INPLACE_NO_LOCK` を返さない。** 排他 MDL の窓は prepare 直前と commit 直前の 2 回開く。INSTANT も commit 前に X を取る | [ALGORITHM と LOCK](./alter-algorithm-selection/) / [ALTER TABLE](./ddl-walkthrough/) | `metadata_locks` の `OBJECT_TYPE = 'TABLE'`                                           |
| `ERROR 1799` (`ER_INNODB_ONLINE_LOG_TOO_BIG`)       | ALTER 中の DML が `innodb_online_alter_log_max_size` を超えた                                                                                | [INPLACE と row log](./online-index-build-row-log/)                                   | エラーメッセージ                                                                      |
| `ALTER` が COPY になってしまう                      | `old_alter_table` が ON、または操作が INPLACE 非対応。**`alter_algorithm` 変数は 8.4 に存在しない**                                          | [ALGORITHM と LOCK](./alter-algorithm-selection/)                                     | `ALGORITHM=INSTANT` を明示して `ER_ALTER_OPERATION_NOT_SUPPORTED_REASON` の理由を読む |
| `ER_INNODB_MAX_ROW_VERSION` (4092)                  | INSTANT な ADD/DROP COLUMN が 64 回に達した                                                                                                  | [INSTANT の実体](./instant-ddl-row-versions/)                                         | `I_S.INNODB_TABLES.TOTAL_ROW_VERSIONS`                                                |
| DDL がクラッシュ後に半端な `.ibd` を残す (残さない) | `mysql.innodb_ddl_log` を replay して後始末する                                                                                              | [アトミック DDL](./atomic-ddl-and-ddl-log/)                                           | エラーログの `POST_RECOVER_DDL_LOG` 系メッセージ                                      |
| マイグレーションが部分的に確定した                  | DDL は implicit commit を伴う。複数 DDL は 1 トランザクションにならない                                                                      | [トランザクションの調停](./transaction-coordination/)                                 | —                                                                                     |
| FTWRL が返ってこない                                | `MDL_key::COMMIT` を待っている                                                                                                               | [トランザクションの調停](./transaction-coordination/) / [MDL](./metadata-locking/)    | `metadata_locks` の `OBJECT_TYPE = 'COMMIT'`                                          |

## ストレージと I/O

| 症状                                   | 仕組み                                                                                                                                                                                              | ページ                                                                                                                     | 確認するビュー                                                                |
| -------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------- |
| `Row size too large (> 8126)`          | 8126 = `(16384 - 120 - 8 - 4) / 2`。1 ページに 2 行入る必要がある                                                                                                                                   | [レコードの構造](./record-format/) / [ページの構造](./page-layout/)                                                        | `SHOW CREATE TABLE` の `ROW_FORMAT`                                           |
| バッファプールのヒット率が下がった     | 全表スキャンで warm cache が押し出された。midpoint 挿入は 512 ブロック未満では無効                                                                                                                  | [LRU と midpoint](./lru-and-midpoint/)                                                                                     | `SHOW ENGINE INNODB STATUS` の `Buffer pool hit rate` / `young-making rate`   |
| 書き込みが周期的に止まる               | redo capacity が足りず同期 flush が入っている                                                                                                                                                       | [チェックポイント](./checkpoint/)                                                                                          | エラーログの `Redo log is running out of free space, pausing user threads...` |
| `Innodb_log_waits` が増える            | ログバッファが足りず、書き込み側が待たされている                                                                                                                                                    | [log writer / flusher](./log-writer-threads/)                                                                              | `innodb_log_buffer_size`                                                      |
| ダーティページが減らない               | page cleaner が `innodb_io_capacity` (8.4 既定 **10000**) と `innodb_max_dirty_pages_pct` の間で追いついていない                                                                                    | [flush list と page cleaner](./flush-list-and-page-cleaner/)                                                               | `Innodb_buffer_pool_pages_dirty` / エラーログの `Page cleaner took NNNNms`    |
| 8.0 のチューニング記事が当てはまらない | 8.4 で既定が変わっている: `innodb_change_buffering=none`、`innodb_adaptive_hash_index=OFF`、`innodb_io_capacity=10000`、`innodb_flush_neighbors=0`、`innodb_flush_method` 未指定なら O_DIRECT probe | [change buffer](./change-buffer/) / [adaptive hash index](./adaptive-hash-index/) / [読み込みと I/O](./read-ahead-and-io/) | `SHOW GLOBAL VARIABLES`                                                       |
| メモリが二重に使われている             | `innodb_flush_method` が O_DIRECT でないとページキャッシュとバッファプールに同じデータが載る                                                                                                        | [読み込みと I/O](./read-ahead-and-io/)                                                                                     | `SHOW ENGINE INNODB STATUS` の `Using Linux native AIO`                       |
| JSON の部分更新が効かない              | `binlog_row_value_options=PARTIAL_JSON` と、LOB が in-place で収まる条件の両方が要る                                                                                                                | [JSON](./json-storage-and-partial-update/) / [LOB](./lob-storage/)                                                         | `binlog_row_value_options`                                                    |
| `.frm` がない                          | 8.0 以降メタデータは `mysql.ibd` の DD に入っている                                                                                                                                                 | [データディクショナリ](./data-dictionary/)                                                                                 | `I_S` (DD 上の view)                                                          |
| `Table_open_cache_overflows` が増える  | `table_open_cache` の LRU から追い出されている                                                                                                                                                      | [データディクショナリ](./data-dictionary/)                                                                                 | `Opened_tables` / `Opened_table_definitions`                                  |

## パーティショニング

| 症状                                                   | 仕組み                                                                  | ページ                                | 確認するビュー                        |
| ------------------------------------------------------ | ----------------------------------------------------------------------- | ------------------------------------- | ------------------------------------- |
| pruning が効かない                                     | `WHERE YEAR(col) = 2026` のように列が関数に包まれている                 | [パーティショニング](./partitioning/) | `EXPLAIN` の `partitions` 列          |
| パーティションキーを含まない UNIQUE が作れない         | すべての一意キーがパーティションキーを含む必要がある (prefix 列は不可)  | [パーティショニング](./partitioning/) | `ER_UNIQUE_KEY_NEED_ALL_FIELDS_IN_PF` |
| パーティション数が多いと `ORDER BY ... LIMIT 1` が遅い | 使用パーティション数 × 1 行ぶんのバッファを毎回確保する優先度付きキュー | [パーティショニング](./partitioning/) | `EXPLAIN` の `partitions` の数        |

## レプリケーション

| 症状                                           | 仕組み                                                                                                                         | ページ                                                                                                 | 確認するビュー                                                                             |
| ---------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------ |
| レプリカが遅れる                               | receiver の遅れと applier の遅れは別物。`Seconds_Behind_Source` は両方を 1 つの数字に潰す                                      | [レプリカ遅延](./replication-lag/)                                                                     | `performance_schema.replication_applier_status_by_worker` の timestamp 列                  |
| `Seconds_Behind_Source` が 0 なのに遅れている  | `last_master_timestamp == 0` なら 0 を返す (NULL ではない)。MTA では GAQ 退出時にしか進まない                                  | [レプリカ遅延](./replication-lag/)                                                                     | 同上                                                                                       |
| 並列適用が効かない                             | `last_committed` / `sequence_number` が直列を示している。source 側の依存追跡は 8.4 で **WRITESET 固定**                        | [applier と並列適用](./applier-and-mta/)                                                               | `Waiting for preceding transaction to commit`                                              |
| 長いトランザクション 1 本で worker が詰まる    | `replica_preserve_commit_order` で後続がコミット順を待つ                                                                       | [applier と並列適用](./applier-and-mta/)                                                               | `replication_applier_status_by_worker`                                                     |
| read-your-writes が壊れる                      | GTID で待たないと reader が追いついていない                                                                                    | [GTID](./gtid/)                                                                                        | `WAIT_FOR_EXECUTED_GTID_SET` / `session_track_gtids`                                       |
| `ER_SOURCE_HAS_PURGED_REQUIRED_GTIDS`          | source 側で必要な binlog が purge された                                                                                       | [GTID](./gtid/)                                                                                        | `gtid_purged` / `binlog_expire_logs_seconds`                                               |
| replica が止まると source の全書き込みが止まる | 半同期の AFTER_SYNC は commit stage の mutex を持ったまま待つ。**待っているセッションだけでなく commit pipeline 全体が止まる** | [半同期](./semi-sync/)                                                                                 | `Rpl_semi_sync_source_status` / `Rpl_semi_sync_source_no_tx`                               |
| CDC で列名が取れない                           | `binlog_row_metadata` の既定 MINIMAL では Table_map に列名が入らない                                                           | [binlog イベント](./binlog-events/)                                                                    | `binlog_row_metadata`                                                                      |
| CDC が中身を読めない                           | `Transaction_payload_log_event` (zstd) は展開しないと中の Table_map / Rows が見えない                                          | [binlog イベント](./binlog-events/)                                                                    | `binlog_transaction_compression`                                                           |
| binlog を有効にしたら書き込みが遅くなった      | 参加者が 2 つになり 2PC が走る (`rw_ha_count > 1`)。設定では消せない構造的コスト                                               | [2PC とグループコミット](./two-phase-commit-and-group-commit/) / [UPDATE の一生](./life-of-an-update/) | `performance_schema` の `LOCK_log` / `LOCK_sync` 待ち (`Binlog_commits` は MySQL にはない) |
| `Binlog_cache_disk_use` が増える               | `binlog_cache_size` を超えて一時ファイルに落ちている                                                                           | [binlog](./binlog-walkthrough/)                                                                        | `Binlog_cache_use` との比                                                                  |
| クラッシュ後に source とレプリカがずれた       | `sync_binlog=1` と `innodb_flush_log_at_trx_commit=1` の**両方**が要る                                                         | [2PC とグループコミット](./two-phase-commit-and-group-commit/)                                         | 両変数                                                                                     |

## Aurora

| 症状                                            | 仕組み                                                                                      | ページ                                              | 確認するビュー                |
| ----------------------------------------------- | ------------------------------------------------------------------------------------------- | --------------------------------------------------- | ----------------------------- |
| チューニングパラメータが変更できない / 効かない | redo をストレージ側に送る設計なので、page cleaner / doublewrite / checkpoint 系が存在しない | [Aurora は何を差し替えたか](./aurora-what-changed/) | Aurora の適用外パラメータ一覧 |
| `Seconds_Behind_Source` が使えない              | reader は binlog ではなく共有ストレージ + redo 適用なので、その経路自体がない               | [Aurora は何を差し替えたか](./aurora-what-changed/) | `AuroraReplicaLag`            |
| `innodb_flush_log_at_trx_commit` の挙動が違う   | Aurora のバージョンで値の意味が変わる (v3 では `2` が `1` と同じ)                           | [Aurora は何を差し替えたか](./aurora-what-changed/) | エンジンバージョン            |

## 使い方

**症状が表にないときは、層で切る。** [SELECT の一生](./life-of-a-select/)と[UPDATE の一生](./life-of-an-update/)に 6 段の切り分け表がある。「同じクエリが速いときと遅いときがある」なら実行段、「`EXPLAIN` の `rows` が実際と桁違い」なら最適化段、というふうに。

**ビューの側から入るなら**、[EXPLAIN の列](./explain-columns/)・[SHOW ENGINE INNODB STATUS](./innodb-status-sections/)・[ログとステータス変数](./logs-and-status-variables/)の 3 ページが、それぞれ「この列/セクション/カウンタは、どの構造体の何を映しているか」の対応表になっている。**表の逆向き** — 見えている数字から仕組みに戻る — はこの 3 ページで引く。

**ソースまで降りるなら**、[ディレクトリ地図](./directory-map/)で層を選び、[ビルドとデバッガ](./build-and-debug/)のブレークポイント一覧から入る。8.0 時代の名前で grep して見つからないときの対応表も[ディレクトリ地図](./directory-map/)にある。
